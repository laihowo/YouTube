name: Life Server

on:
  schedule:
    #- cron: '*/5 * * * *'

jobs:
  send-request:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request
        run: |
          curl https://life-server-abcxyz-8080.preview.app.github.dev/
